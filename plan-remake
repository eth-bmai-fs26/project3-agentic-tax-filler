Todos left:
- Make sure that the student has to fill manually at least one of the plans (income plan)
- Make them fill the user_msg in build_plan_user_message
- Add a hint markdown before the think function to remind them to look at the diagram earlier
- Collapse def visualize_plan cell
- in think_loop, extract the html visualization block and put them in some functions that are called instead of flooding the function.
- Make them fill page_data = ... (with the comments there)
- Before the think step cell, make sure to include a markdown cell with the schema of the functions that they must used and haven't seen yet (such as fill page)